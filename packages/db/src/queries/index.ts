/**
 * Stub barrel for query helpers. The Foundation worktree (W1) replaces this
 * with concrete async functions (`getPackBySlug`, `findActiveTokenByHash`,
 * etc.) per the ISC list in ISA.md § Iteration-4 / Phase 3.A.
 */
export {};
