agentpack: "1.0"

metadata:
  id: "agentpack.partial-target-fixture"
  name: "Partial Target Fixture"
  slug: "partial-target-fixture"
  description: "Test fixture: authored compatibility declares the generic target PARTIAL — the --allow-partial-target gate (#134) must fire."
  version: "0.1.0"
  license: "MIT"
  publisher: "agentpack"
  authors:
    - name: "AgentPack"
      email: "hello@agentpack.dev"
  tags:
    - test

compatibility:
  targets:
    generic:
      status: partial
      notes: "Hooks are dropped on this target."

permissions:
  filesystem:
    read:
      - "."
  package_installation: false
  model_provider_key_access: false

security:
  risk_level: low
  risk_summary: "Low — instruction content only."
  requires_review: false
  signed: false

profiles:
  full:
    description: "Includes the instruction."
    include:
      - "*"

atoms:
  - id: "instruction:house-style"
    type: instruction
    name: "House Style"
    description: "A persistent instruction with no executable surface."
    path: "atoms/instructions/house-style.md"
    risk_level: low
    permissions: []

exports:
  default_profile: full
  output_dir: "dist"
  lockfile: "AGENTPACK.lock"

adapters:
  generic:
    enabled: true
    output:
      instructions: "AGENTS.md"
      skills: "skills"
      manifest: "agentpack.json"
      readme: "README-agent.md"
