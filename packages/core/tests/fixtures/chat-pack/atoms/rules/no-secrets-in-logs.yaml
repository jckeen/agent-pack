id: no-secrets-in-logs
name: No Secrets In Logs
severity: required
behavior:
  must:
    - "Redact tokens, passwords, and API keys before logging."
  must_not:
    - "Print environment variables that may contain credentials."
