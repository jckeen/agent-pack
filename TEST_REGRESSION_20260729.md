# Test Regression Report — agent-pack — 2026-07-29

**Summary (built-then-tested, as CI runs it):** 2 failed / 48 passed in `@agentpack/connector`; all other packages green — `@agentpack/core` 707 passed, `@agentpack/db` 79 passed.
**Commit tested:** `16d4320c6d9757f34143e62a3ebf5698a1e8d1fe` (HEAD of default branch)

**Classification:** ⚠️ Flaky (test-isolation / sandbox networking), **not a confirmed code regression.**

## Methodology note (important)

A bare `pnpm test` **without a preceding `pnpm build`** produces spurious failures:
`Error: Failed to resolve entry for package "@agentpack/core"` in every connector test,
because `@agentpack/core`'s `exports` point to `./dist/**` which does not exist until built.
CI runs `pnpm build` before `pnpm test:coverage` (`.github/workflows/ci.yml`), so this is a
harness artifact, not a repo bug. **The results below are after `pnpm build && pnpm test`.**

## Failing tests (after correct build)

Both in `packages/connector/tests/serve.test.ts`:

1. **`serve.ts entrypoint > binds the server and prints the startup banner with a valid token`**
   - `Error: Test timed out in 5000ms.` at `tests/serve.test.ts:45`
   - The test sets `AGENTPACK_CONNECTOR_PORT="0"` and awaits a real server bind; the listen callback never resolves within 5s under full parallel load.

2. **`serve.ts entrypoint > defaults the port to 8787 when AGENTPACK_CONNECTOR_PORT is unset`**
   - `AssertionError: expected "vi.fn()" to be called 1 times, but got 2 times` at `tests/serve.test.ts:93`
   - Cascade from test #1: the prior test's late-resolving `serve()` mock call bleeds into this assertion.

## Why this is flaky, not a regression

- `serve.test.ts` **passes 4/4 in isolation** (`npx vitest run tests/serve.test.ts`, exit 0). It only fails inside the full recursive `pnpm -r` run.
- Failure #1 is a real network bind timing out in the sandbox; #2 is a direct isolation cascade from it.
- Commits in the last 24h: **#183** (dependency bump, 19 minor/patch updates), #182 (docs), #173 (docs). Only #183 could plausibly touch runtime behavior, but the isolation-pass evidence points to test flakiness under parallel load rather than a code change in #183.

## Recommendation

Report-only; no rollback. Harden `serve.test.ts`: raise the timeout for the real-bind case, ensure the server is closed and the `serve` mock reset in `afterEach`, and isolate the entrypoint spec (single-thread) so a slow bind cannot cascade into the next test.

_Generated by an automated nightly test-regression routine (report-only)._
